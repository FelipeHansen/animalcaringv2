<%= form_for(veterinario) do |f| %>
  <% if veterinario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(veterinario.errors.count, "error") %> prohibited this veterinario from being saved:</h2>

      <ul>
      <% veterinario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="field">
    <%= f.label :cpf %>
    <%= f.text_field :cpf %>
  </div>

  <div class="field">
    <%= f.label :clinica %>
    <%= select(:veterinario, :clinica_id, Clinica.all.collect {|c| [c.nome, c.id]}) %>
  </div>
  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
