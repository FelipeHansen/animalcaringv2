<%= form_for(consulta_medicamento) do |f| %>
  <% if consulta_medicamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(consulta_medicamento.errors.count, "error") %> prohibited this consulta_medicamento from being saved:</h2>

      <ul>
      <% consulta_medicamento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :consulta_id %>
    <%= f.text_field :consulta_id %>
  </div>

  <div class="field">
    <%= f.label :medicamento_id %>
    <%= f.text_field :medicamento_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
