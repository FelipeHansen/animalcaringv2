<%= form_for(consulta) do |f| %>
  <% if consulta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(consulta.errors.count, "error") %> prohibited this consulta from being saved:</h2>

      <ul>
      <% consulta.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data_consulta %>
    <%= f.text_field :data_consulta %>
  </div>

  <div class="field">
    <%= f.label :descricao %>
    <%= f.text_field :descricao %>
  </div>

  <div class="field">
    <%= f.label :animal_id %>
    <%= select(:consulta, :animal_id, Animal.all.collect {|c| [c.nome, c.id]}) %>
  </div>

  <div class="field">
    <%= f.label :veterinario_id %>
    <%= select(:consulta, :veterinario_id, Veterinario.all.collect {|c| [c.nome, c.id]}) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
